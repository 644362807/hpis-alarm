# 报警第一阶段优化变更总览

## 文档定位

本文是 `hpis-alarm` 第一阶段报警分片与消警可靠化优化的中文总入口。后续阅读优先看本目录下中文命名文档，旧英文命名文档保留为历史草案和配套记录，不再作为最新口径的唯一来源。

## 第一阶段完成内容

本阶段围绕“报警可分片、消警不丢、压测可定位、空闲少打扰、批量链路可回滚”完成了八类改造：

1. 时间 + 容量分片：业务表从固定 `alarm_id % 5` 转向 `alarm_yyyyMM_nn` 形式，按报警开始时间定位月份，按容量阈值切月内子表。
2. `alarm_id` 路由化：内部 `alarm_id` 保持 `bigint`，编码中包含日期、slice、worker、rowNo 等信息，用于按 ID 直达分片。
3. cid 热点路由：`alarm_cid_index` 只服务 active/hot 生命周期；长期历史不再通过 cid 快速定位，避免全量路由表无限增长。
4. stop 可靠化：MQ stop 先落 `alarm_stop_event`，后台 worker 批量关闭业务表、cid route 和 event 状态，side effect 独立执行。
5. 运行降噪：stop worker 空闲后暂停数据库轮询，收到新 stop 后唤醒；SQL 日志改为 `alarm.sql-log.*` 分层开关。
6. insert/stop 批量化：新增 `alarm.batch.*` 开关，stop route 查询从循环单查改为 `IN` 批量查询，insert 新增内部 `insertAlarms` 批量 API；外部 MQ/Controller 单条入口和 push 消息体保持不变。
7. MQ start consumer batch：新增 `RabbitMQAlarmBatchListener`，用 Spring AMQP 原生 consumer batch 收集一批消息后批量持久化 start；同批 start 先落库，再处理 stop/status，ack/nack 仍逐条执行。
8. SQL 批处理边界治理：新增统一 `AlarmBatchChunker`，把 `IN`、`CASE WHEN`、多 values `INSERT` 和异常逐条降级限制在可控范围；涉及物理表时固定先按 suffix 分组，再在分片内切块。
9. stop 闭环解压：新增 `PROCESSING + lock_token` 原子认领和 stop 专用线程池，移除新 worker 热路径中的全量 `countPending`。

## 中文主文档

| 文档 | 用途 |
| --- | --- |
| `报警分片数据链路说明.md` | 从 MQ 接收、开始报警、停止报警、路由、清理任务角度说明完整数据流。 |
| `报警时间容量分片设计说明.md` | 说明 `yyyyMM_nn` 分片规则、Nacos 配置、绑定表、hot/stale cid 路由职责。 |
| `报警ID与容量预占设计说明.md` | 说明 `alarm_id` v2 编码、Segment 预占、`alarm_shard_slice.current_rows` 语义。 |
| `报警停止可靠化与批量关闭设计说明.md` | 说明 `alarm_stop_event`、批量关闭、side effect、幂等和高低流量策略。 |
| `报警压测与Profiling测试报告.md` | 汇总 1000/20000 双场景压测、profiling 数据和剩余瓶颈。 |
| `报警运行配置说明.md` | 说明 `alarm.sharding`、`alarm.stop-worker`、`alarm.batch`、`alarm.sql-log`、`alarm.profile` 的生效情况、默认值和执行入口。 |
| `报警Push接口测试用例-全新环境.md` | 提供可导入 Postman/Apifox 或 HTTP Client 的报警创建 10、工单创建 25 接口用例、初始化清理 SQL 和本地 HTTP 接收器。 |
| `../agent.md` | 说明后续 Agent 修改代码时的中文注释、专门修改文档、资料同步、低风险重构和提交范围要求。 |
| `本轮修改记录-20260523-agent规范与文档追加.md` | 记录 2026-05-23 新增 `agent.md` 和文档索引的背景、范围、风险、验证与回滚方式。 |
| `本轮修改记录-20260523-批量链路中文注释补强.md` | 记录 2026-05-23 对前两轮批量链路关键类和函数补充中文注释的范围、风险边界、验证与回滚方式。 |
| `本轮修改记录-20260523-清理自研聚合保留consumer-batch.md` | 记录 2026-05-23 清理自研 MQ start 聚合、保留旧单条 listener 与 Spring AMQP consumer batch、修正同批 start/stop 顺序的范围和验证。 |
| `本轮修改记录-20260524-ROUTE_MISSING可重试与压测计时修正.md` | 记录 2026-05-24 将 `ROUTE_MISSING` 改为可重试/可恢复、补充 stop worker 指标、修正压测计时口径和 Maven 测试规则。 |
| `报警SQL循环与批处理风险治理计划-20260530.md` | 记录循环 SQL、超大批量 SQL 风险台账，第一阶段治理状态和第二阶段 stop 闭环待处理项。 |
| `本轮修改记录-20260530-SQL循环与批处理边界治理.md` | 记录 2026-05-30 统一 chunk、异常降级保险丝和 SQL 批处理边界治理。 |
| `本轮修改记录-20260531-stop-worker-PROCESSING认领.md` | 记录 2026-05-31 stop worker 短事务认领、专用线程池、迁移脚本和灰度门禁。 |
| `本轮修改记录-20260602-stop可靠性审核修复.md` | 记录 2026-06-02 PROCESSING 重试上限、单条快照模式一致性和 start 后到补偿 outbox 原子性修复。 |
| `测试报告-报警分片节点刷新与月末预热.md` | 记录 2026-06-08 actualDataNodes 刷新、快速预热、真实 CRUD 接口回归和合并结论。 |
| `合并版本迭代记录.md` | 按合并版本记录进入 `main` 的功能范围、提交号、验证结果、配置影响和遗留风险。 |

## 本阶段关键代码入口

| 入口 | 说明 |
| --- | --- |
| `RabbitMQAlarmListener` | MQ start/stop 消息入口，stop 消息写入 `alarm_stop_event` 后 ack。 |
| `RabbitMQAlarmBatchListener` | 可选 MQ batch 消费入口，`insertConsumerBatchEnabled=true` 时接管 `alarm_queue`，start 批量持久化，stop/status 单条分流。 |
| `AlarmServiceImpl.insertAlarm` | 开始报警主流程，负责解析报警、分配分片、写业务表和 hot cid route。 |
| `AlarmServiceImpl.insertAlarms` | 内部批量新增报警 API，按 suffix 批量持久化，受 `alarm.batch.insertEnabled` 控制。 |
| `AlarmInsertBatchConsumeService` | batch listener 的 start 批处理服务，返回每条消息的 `SUCCESS/SKIP/DROP/FAIL` 结果，ack/nack 仍由 listener 执行。 |
| `AlarmCidIndexService` | hot/stale cid 路由生命周期管理，包含批量关闭 route。 |
| `AlarmStopEventService` | stop event 可靠落库、批量 route 查询、批量处理和 start 后补偿。 |
| `AlarmStopEventClaimService` | 原子认领 `PENDING`、释放失败批次、回收超时 `PROCESSING`。 |
| `AlarmStopSideEffectService` | 设备恢复、EC 清理、推送等非核心副作用事件。 |
| `AlarmStopWorkerSignal` | stop worker 空闲暂停、入库唤醒和低频兜底扫描状态控制。 |
| `AlarmMonthlySliceTableManager` | 月份容量 slice 分配、Segment 预占和动态建表。 |
| `CustomSqlLogInterceptor` | SQL 日志分层打印，默认关闭全量 SQL info，避免 worker 空转刷屏。 |
| `AlarmProfiler` | 本机压测阶段方法级/SQL 聚合耗时统计，当前 `alarm.profile.*` 没有运行入口。 |

## 当前边界

- 当前不删除业务报警历史，只清理 hot/stale cid 路由和已完成 stop event。
- cid 只服务 active/hot 生命周期；关闭后过一段时间会被物理清理。
- 长期历史查询应走内部 `alarm_id` 或时间范围。
- stop 允许秒级最终一致，但最终必须写入 `alarm_endTime`。
- side effect 可以延迟，但不能阻塞核心消警。
- stop worker 默认连续 3 轮空处理后进入 idle pause，靠新 stop 入库唤醒，并保留 60 秒兜底扫描。
- stop 批量链路中 true route 缺失会先按 `available_time` 延迟回到 `PENDING`，达到重试上限后才转 `FAILED/ROUTE_MISSING`；历史误失败仍保留恢复扫描。
- `insertAlarm(JSONObject)` 外部入口不变；`insertAlarms(List<JSONObject>)` 是内部扩展 API，批量失败会回滚批量事务后逐条兜底。
- MQ start consumer batch 默认关闭；开启 `alarm.batch.insertConsumerBatchEnabled=true` 后旧 `RabbitMQAlarmListener` 条件关闭，`RabbitMQAlarmBatchListener` 接管同一个 `alarm_queue`。
- consumer batch 第一阶段只批量 start 持久化；同批 start 会先处理，同批 stop/status 随后按旧单条链路处理并逐条 ack/nack。
- consumer batch 不移动 `breakLine(...)` / `breakLineUnitAction(...)` 到 afterCommit；批量失败拆单时复用 context，避免重新组装导致额外重复触发，但首次批量尝试前的远程副作用仍可能早于 DB 提交。
- 旧单条 listener 和 consumer batch 都不保证同一 `alarmCid` 严格顺序，沿用当前 Rabbit 并发消费下的最终一致语义。
- SQL info 默认不打印；生产临时排查时通过 `alarm.sql-log.enabled=true` 和 `mode` 控制范围。
- 多实例部署必须为每个实例配置不同 `alarm.sharding.id.workerId`。
- 后续每轮代码修改必须遵守根目录 `agent.md`：关键业务代码补充中文风险注释，并新增专门修改文档；本约束只新增协作规范，不改变运行期行为。

## MQ start consumer batch 风险点与迭代计划

> 2026-05-21 1000 条 start-only 压测显示，历史 `AlarmInsertAggregateService.submitAndWait` 方案功能可用但吞吐不达预期：listener 线程同步等待导致实际 batchSize 被当前消费者数限制，配置 200 时实际多为 10。2026-05-23 已清理该自研聚合器，后续只保留旧单条 listener 与 Spring AMQP consumer batch 两条入口。
>
> 2026-05-22 1000 条 start-only 压测显示，Spring AMQP consumer batch 方案功能通过：`insertConsumerBatchEnabled=true`、`batchSize=100`、`receiveTimeoutMs=50`、`concurrency=2-4`、`prefetch=100` 时，实际 10 批、每批 100 条，`ack=1000`、`nack=0`，PERSIST 首尾耗时 2723ms，估算 367.24/s。对比旧单条 listener 104.22/s、旧 submitAndWait 聚合 38.76/s，第一阶段生产灰度应优先走 consumer batch。

### 风险点

1. Redis 去重复用：断线告警在 prepare 阶段执行 `tryAcquire`；批量事务失败后必须复用 `AlarmInsertContext` 拆单，不能重新 prepare，否则会错误跳过或重复触发副作用。
2. 批量事务回滚：跨多个 suffix 的 batch flush 是一个事务，任一 suffix 写失败会整批回滚；`fallbackSingleOnBatchError=false` 时整批返回 FAIL 并 requeue。
3. 远程副作用允许重复触发：第一阶段不移动 `breakLine`、`breakLineUnitAction` 到 afterCommit，批量失败和 MQ 重投场景仍可能重复触发远程状态同步，业务已接受该边界。
4. Rabbit 并发顺序：旧 listener `concurrency="10-36"` 或 batch listener `insertConsumerBatchConcurrency` 下，同一 `alarmCid` 的 start 不保证严格顺序，不新增同 cid 串行化。
5. push 兼容性：batch 成功后仍逐条 afterCommit 发布原 JSON 结构；如果未来改 payload，必须另开兼容性评审。
6. consumer batch 混合消息：同一个 batch 可能同时包含 start、stop、status；当前实现先处理 start，再处理 direct 消息，降低 stop 先查不到 route 的误判窗口。
7. consumer batch ack 粒度：只要存在部分成功/失败，就必须逐条 ack/nack，不能使用 `multiple=true` 批量确认。
8. consumer batch 配置误差：`prefetch < batchSize` 或 `receiveTimeoutMs` 过小会导致实际 batchSize 偏小，必须用日志观测 `actualBatchSize`。

### 迭代计划

1. 第一阶段：`insertConsumerBatchEnabled=false` 默认关闭，灰度时手动改 true；`batchSize=100`、`receiveTimeoutMs=50`、`concurrency=2-4`、`prefetch=100` 起步，已完成 1000 条 start-only 验证。
2. 第二阶段：如果 consumer batch 吞吐达标，再接 MQ backlog 或消费 lag 指标，决定何时开启 batch listener 或调整 batch 参数。
3. 第三阶段：批量设备解析、报警配置解析和 Redis 读优化，减少 prepare 阶段逐条远程/缓存访问。
4. 第四阶段：压测后再评估多分区 consumer batch 或 backlog 自适应参数；不再恢复自研内存 submitAndWait 聚合器。

## 后续优化方向

压测显示本阶段已把逐条 `closeRoute`、`markApplied`、`markDone` 降为几十批，剩余主要瓶颈不再是这些逐条更新，而是：

- RabbitMQ listener 消费节奏和并发配置。
- start 插入链路仍存在多表写入成本，电解槽场景额外写扩展表更明显。
- 内部 `insertAlarms` 已支持按 suffix 批量持久化；MQ 高峰期优先通过 Spring AMQP consumer batch 调用批量持久化。
- stop event 每条 stop 消息仍需要一次可靠 upsert。
- 电解槽 `EC_ECTYPE_DELETE` 副作用量大，后续可考虑按 alarmId 批量执行。
- 空闲暂停只降低无任务时的数据库轮询，不能解决高峰期 MQ 消费和 stop upsert 吞吐问题。

## 2026-05-24 alarm_id 前置修复索引

- `本轮修改记录-20260524-alarm-id前置与电解槽复测.md`：记录内部 `alarm_id` 生成前置、电解槽扩展表统一绑定主表 ID、完整电解槽报警复测和剩余风险。

## 2026-06-01 2w MQ/min 最小优化索引

- `本轮修改记录-20260601-2wMQ每分钟最小优化.md`：记录 stop 连续消息聚批、最新 `stop_time` 一致性、token/version 门禁、电解槽快照有限重试、可靠 outbox 和压测灰度参数。

## 2026-06-02 stop 可靠性审核修复索引

- `本轮修改记录-20260602-stop可靠性审核修复.md`：记录 PROCESSING 异常释放和超时回收的有限重试、电解槽单条 fallback 的快照模式一致性、start 后到补偿 outbox 失败回滚，以及暂缓治理的并发风险。
## 2026-06-04 分片节点刷新索引

- `本轮修改记录-20260604-分片节点刷新与目录文档.md`：记录 ShardingSphere 4.1.1 actualDataNodes 刷新改造、legacy `0..4` 清理边界、测试范围和回滚关注点。
- `项目目录功能说明.md`：维护 `hpis-alarm` 项目目录、核心类职责和本轮分片刷新入口，供后续快速了解架构。
- 新增关键入口：`AlarmShardingDataSourceFactory`、`RefreshableAlarmShardingDataSource`、`AlarmShardingRuleRefreshJob`。
- 当前规则：历史月份按实际 `yyyyMM_nn` 表注册；当前月预注册 `00..255`；下月默认预注册 `00..09`；旧 `alarm_0..4` 系列表不再注册、不再兜底查询。

## 2026-06-05 分片建表后主动刷新索引

- `本轮修改记录-20260605-分片建表后主动刷新.md`：记录动态建表后主动刷新、afterCommit、去抖、单飞和失败保留旧 DataSource 的实现边界。
- 新增关键入口：`AlarmShardingRuleRefreshService`，定时刷新和主动刷新统一通过该服务执行。
- 当前边界：主动刷新只在真实创建新物理表后触发，不暂停 MQ，不改变业务增删改查语义，不替代 actualDataNodes 预注册。

## 2026-06-07 月末预热与下月节点注册索引

- `本轮修改记录-20260607-月末预热与下月节点注册.md`：记录启动只预建当前月 `00`、下个月默认注册 `00..09`、月末创建下个月 `00` 并显式刷新 DataSource 的新策略。
- 新增关键入口：`AlarmShardingNextMonthPreCreateJob`，每天 `23:50` 触发并在代码内判断月末。
- 当前边界：按单实例运行设计，不引入分布式锁；月末预热刷新不走建表后防抖。

## 2026-07-14 Alarm → Push 全流程基线修复索引

- 计划：`docs/superpowers/plans/2026-07-14-alarm-push-e2e-baseline-fix-plan.md`。
- 统一接口测试与结果：`hpis-alarm/doc/报警Push接口测试用例-全新环境.md`。
- 修改记录：`hpis-alarm/doc/本轮修改记录-20260714-Alarm-Push全流程基线修复.md`。
- Alarm 配置统一支持所有报警类型通过 `deviceIds` 绑定当前租户设备；列表、详情、更新、删除均增加当前租户边界。
- Alarm 按 `tenantId + sceneType + deviceSn + alarmType` 匹配服务端配置，默认只在匹配且 `pushEnabled=1` 时发送配置推送；兼容回滚开关为 `alarm.push.require-matched-config=false`。
- Push 配置 CRUD、pushKey 绑定/解绑强制当前租户；HTTP/MQTT Consumer 在更新、禁用、删除时可显式停止，启动时从 DB 启用配置重建运行态。
- 修复 Push 新增配置 `del_flag=NULL` 导致新增后不可见的问题；新数据显式写 `0`，读取层兼容历史空值。
- 2026-07-14 实测运行号 `20260714225038`：Alarm/Push CRUD、HTTP 报警、真实 `alarm_queue`、HTTP 最终接收、禁用停止投递、租户隔离和 API 清理全部 PASS。
- 企业微信、`pushBindingId`、推送绑定组和工单转派不在本轮实现范围。
