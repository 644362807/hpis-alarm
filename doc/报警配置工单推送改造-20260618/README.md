# 报警配置工单推送改造文档索引

本文档目录用于记录报警配置、报警工单、报警推送细分改造的迭代方案、变更范围、风险评审和验证计划。

本目录只记录计划和审核，不修改 Java、XML、SQL、配置文件，也不代表代码已经实现。

其中 `10-最终设计口径确认.md`、`12-操作逻辑字段使用与数据流转详细设计.md` 和 `13-报警确认工单闭环详细设计方案.md` 是当前最终口径；早期文档中关于 `alarm.workorder_id`、`alarm.alarm_configure_id`、`config_version`、自动创建工单等描述，以这三份文档为准进行修正。

## 适用范围

- `hpis-alarm` 报警配置、报警产生、报警处理、报警工单链路。
- `hpis-push` 推送配置、Redis 路由、设备级和行业级推送匹配链路。
- 数据库表范围以《报警配置最小改动方案设计文档》为主。

## 文档索引

| 文件 | 内容 |
| --- | --- |
| `01-任务迭代路线.md` | 三阶段迭代路线、每期目标、输入输出、完成标准和暂不做事项。 |
| `02-变更范围确认.md` | 按模块确认后续代码变更范围和明确不改范围。 |
| `03-风险评审.md` | 业务、幂等、数据一致性、Redis、推送、工单、回滚风险。 |
| `04-验证方案.md` | 单元测试、Mapper/SQL 合约、Redis、接口/MQ 联调、灰度指标。 |
| `05-数据模型与路由规则.md` | 字段口径、配置匹配规则、推送路由规则和兼容策略。 |
| `06-实施检查清单.md` | 后续代码实现前、中、后的检查清单。 |
| `07-数据库字段修改说明.md` | 按表详细说明新增或调整字段的功能、写入时机、读取用途和注意事项。 |
| `08-已有文档复核与设计补充建议.md` | 基于已有 Markdown 和 Word 文档的复核结论、风险排序和实现前确认项。 |
| `09-基于现有表结构的字段裁剪评审.md` | 基于现有 SQL 表结构和告警闭环需求，对 `alarm`、`alarm_configure`、`alarm_handle` 字段做裁剪审核。 |
| `10-最终设计口径确认.md` | 记录已确认的最终流程、表职责、字段边界、最小改动可行性和验收口径。 |
| `11-确认后人工创建工单实施计划.md` | 早期实施计划，执行前必须按 `13` 号详细设计补充改派负责人和工单关闭同步等内容。 |
| `12-操作逻辑字段使用与数据流转详细设计.md` | 执行实施计划前，详细说明用户操作流程、字段使用、报警/工单/推送数据流和边界条件。 |
| `13-报警确认工单闭环详细设计方案.md` | 汇总最新 grill 结论，给出报警确认、直接处理、工单创建、改派、关闭同步的最终详细设计。 |
| `14-报警确认工单闭环最终执行计划.md` | 基于 `13` 号详细设计生成的最终代码执行计划，取代 `11` 号旧实施计划作为后续实现入口。 |

## 已确认决策

- 字段口径以《报警配置最小改动方案设计文档》为主。
- `alarm_configure.push_message_type` 表示推送细分，不新增额外推送细分字段。
- `alarm_configure.push_message_type` 对应 `active_push_config.message_type`。
- 报警配置只绑定 `push_message_type` 推送策略编码，不直接绑定单个 `active_push_config_id`。
- 推送配置决定谁收到通知，报警管理权限决定谁能确认报警。
- 行业全部报警用 `alarm_configure.alarm_type` 为空或 `*` 表示。
- 设备级和行业级推送配置同时命中时采用 `UNION`，按 `active_push_config_id` 去重。
- 一条报警最多生成一个 `alarm_workorder` 工单实例。
- 工单转派产生处理或流转记录，不新增第二张工单实例。
- 创建工单时工单模板提供默认负责人，确认人可以临时改派，最终负责人写入 `alarm_workorder`。
- 工单关闭后必须写入 `alarm_handle` 工单同步动作。

## 当前代码事实

- 当前报警服务已有 after-commit 异步推送入口，但消息体尚未携带配置来源和 `push_message_type`。
- 当前推送服务已存在 `deviceSn#messageType` 风格的 Redis 路由，但行业级和全局级路由语义尚未落地。
- 当前工单配置表、工单实例表和工单服务尚未落地。
- 当前 `alarm`、`alarm_handle` 属于分片链路，新增字段必须同步分片建表和 Mapper 批量 SQL。
