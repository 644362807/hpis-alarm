# 实施检查清单

本文件用于后续代码实现前、中、后的检查，不包含代码实现。

## 实施前检查

- [ ] 已确认字段口径以《报警配置最小改动方案设计文档》为主。
- [ ] 已确认不新增额外推送细分字段。
- [ ] 已确认 `push_message_type` 表示推送细分。
- [ ] 已确认设备级和行业级同时命中时采用 `UNION`。
- [ ] 已确认一条报警最多一个工单实例。
- [ ] 已确认转派只写处理或流转记录，不新增第二张工单。
- [ ] 已确认本轮是否只做第一阶段，避免一次性改动过大。
- [ ] 已确认是否需要灰度开关。

## 数据库检查

### alarm_configure

- [ ] 新增 `is_device_sync`。
- [ ] 新增 `config_device_type`。
- [ ] 新增 `device_rule_json`。
- [ ] 新增 `workorder_config_id`。
- [ ] 新增 `push_message_type`。
- [ ] 新增 `config_version`。
- [ ] 新增 `sync_status`。
- [ ] 新增 `last_sync_time`。
- [ ] 新增 `last_sync_result`。
- [ ] Mapper resultMap 覆盖新字段。
- [ ] insert SQL 覆盖新字段。
- [ ] update SQL 覆盖新字段。
- [ ] select SQL 覆盖新字段。

### alarm

- [ ] 新增 `alarm_configure_id`。
- [ ] 新增 `config_version`。
- [ ] 新增 `device_type`。
- [ ] 新增 `alarm_title`。
- [ ] 新增 `alarm_content`。
- [ ] 新增 `workorder_id`。
- [ ] 新增 `rule_snapshot_json`。
- [ ] 新增 `raw_payload_json`。
- [ ] 单条 insert SQL 覆盖新字段。
- [ ] 批量 insert SQL 覆盖新字段。
- [ ] 查询 resultMap 覆盖必要字段。
- [ ] 分片建表逻辑覆盖新字段。
- [ ] 迁移模板记录新字段处理口径。

### alarm_handle

- [ ] 新增 `handle_type`。
- [ ] 新增 `source_type`。
- [ ] 新增 `source_id`。
- [ ] 新增 `status_before`。
- [ ] 新增 `status_after`。
- [ ] 新增 `handle_result`。
- [ ] 新增 `attachments_json`。
- [ ] 单条 insert/update SQL 覆盖新字段。
- [ ] 分片建表逻辑覆盖新字段。

### alarm_workorder_config

- [ ] 创建表。
- [ ] 主键为 `workorder_config_id`。
- [ ] 包含启用、生成方式、派单、超时、自动关闭报警等字段。
- [ ] 增加租户、逻辑删除和审计字段。

### alarm_workorder

- [ ] 创建表。
- [ ] 主键为 `workorder_id`。
- [ ] `workorder_no` 唯一。
- [ ] `alarm_id` 唯一。
- [ ] 包含报警、设备、派单、状态、处理结果等快照字段。
- [ ] 增加租户、逻辑删除和审计字段。

### active_push_config

- [ ] `message_type` 调整为 `varchar(30)`。
- [ ] `push_channel_type` 调整为 `varchar(20)`。
- [ ] 确认是否需要 `bind_ip`。

## 报警服务实现检查

- [ ] 新增配置解析组件。
- [ ] 配置匹配顺序固定为设备+报警类型 > 行业+报警类型 > 行业+全部报警。
- [ ] 配置解析失败不阻断报警入库。
- [ ] 命中结果写入报警上下文。
- [ ] `rule_snapshot_json` 记录命中配置快照。
- [ ] `raw_payload_json` 记录原始报警 JSON。
- [ ] after-commit 推送 payload 增加 `pushMessageType`。
- [ ] after-commit 推送 payload 增加 `alarmConfigureId`。
- [ ] after-commit 推送 payload 增加 `configVersion`。
- [ ] 批量报警和单条 fallback 行为一致。
- [ ] `push.open=false` 时无推送副作用。

## 推送服务实现检查

- [ ] Redis key 构造集中定义。
- [ ] 保留旧 `deviceSn#messageType` 兼容路径。
- [ ] 支持 `deviceSn#pushMessageType`。
- [ ] 支持 `SCENE:{sceneType}#pushMessageType`。
- [ ] 支持 `SCENE:{sceneType}#*`。
- [ ] 设备级、行业级、行业全部报警配置合并后按配置 ID 去重。
- [ ] 无配置命中返回空列表。
- [ ] 无配置命中不抛异常。
- [ ] 删除或禁用配置时清理 Redis。
- [ ] 更新配置时先移除旧路由再写入新路由。

## 工单实现检查

- [ ] 自动生成工单只在 `workorder_config_id` 不为空时执行。
- [ ] `alarm_workorder_config.enabled=1` 才生效。
- [ ] 第一阶段只实现 `create_mode=1`。
- [ ] `alarm_workorder.alarm_id` 唯一约束存在。
- [ ] 唯一键冲突视为幂等成功或可恢复状态。
- [ ] 工单生成成功后回写 `alarm.workorder_id`。
- [ ] 工单状态变化写入 `alarm_handle`。
- [ ] 工单转派不新增第二张工单。
- [ ] 工单关闭按 `auto_close_alarm` 决定是否关闭报警。

## 日志与观测检查

- [ ] 报警配置解析日志包含 `traceId`。
- [ ] 日志包含 `alarmId` 或 `alarmCid`。
- [ ] 日志包含 `tenantId`。
- [ ] 日志包含 `sceneType`。
- [ ] 日志包含 `deviceSn`。
- [ ] 日志包含 `alarmType`。
- [ ] 日志包含 `pushMessageType`。
- [ ] 推送日志包含命中的配置 ID 列表。
- [ ] 工单日志包含 `workorderId`。
- [ ] 异常日志区分配置未命中、Redis 未命中、投递失败、工单幂等冲突。

## 测试检查

- [ ] 配置解析单元测试通过。
- [ ] 推送路由单元测试通过。
- [ ] 工单幂等单元测试通过。
- [ ] Mapper XML 合约测试通过。
- [ ] 分片新建表字段验证通过。
- [ ] 单条报警入库验证通过。
- [ ] 批量 MQ 报警入库验证通过。
- [ ] stop 可靠关闭回归通过。
- [ ] 旧推送 payload 回归通过。
- [ ] 新推送 payload 联调通过。

## 回滚检查

- [ ] 新字段全部 nullable 或有默认值。
- [ ] 新逻辑有开关或可通过配置禁用。
- [ ] Redis 新旧路由可区分清理。
- [ ] 自动工单可通过禁用工单配置关闭。
- [ ] 回滚旧代码时不依赖删除字段。
- [ ] 回滚方案已记录到本轮修改记录。

## 每轮修改后文档检查

- [ ] 新增或更新对应“本轮修改记录”。
- [ ] 记录本轮改动范围。
- [ ] 记录已执行验证。
- [ ] 记录未覆盖风险。
- [ ] 记录回滚方式。
- [ ] 更新本目录相关文档状态。
