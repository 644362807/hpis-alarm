# Alarm / Push 发布验收与回滚清单

## 1. 发布信息

- [ ] 日期、负责人、代码版本和JAR SHA-256已记录。
- [ ] 目标环境、数据库名、Nacos profile已记录。
- [ ] 当前为单实例，Alarm workerId=0。
- [ ] Nacos使用public Namespace、DEFAULT_GROUP。
- [ ] 备份已完成恢复演练。

## 2. 数据库迁移

- [ ] 原始`hpis_alarm1`只读且未执行DDL。
- [ ] 只读预检结果已保存。
- [ ] 孤立/重复/非法时间全部有处理记录。
- [ ] Alarm结构脚本按08号文档顺序执行。
- [ ] 最新迁移run为PASS，audit全部PASS。
- [ ] ID映射CSV数量与数据库映射表一致。
- [ ] Push企业微信增量适用条件已确认。
- [ ] Push消息目录DDL已执行。
- [ ] active配置messageType目录覆盖检查为空。
- [ ] `push-dict-filter-precheck.sql`三个`problem_count`全为0，当前投产messageType不超过5字符。

## 3. 配置和字典

- [ ] `application-prod.yml`、`hpis-alarm-prod.yml`、`hpis-push-prod.yml`已在public/`DEFAULT_GROUP`创建，且未误读`dev` Data ID。
- [ ] `file.path`按操作系统设置且部署账号可写。
- [ ] Alarm/Push数据源指向正确数据库。
- [ ] 已确认Alarm新分片数据源当前使用Hikari库默认池；不将旧Druid `max-active`配置误当为已生效。
- [ ] Redis、RabbitMQ连接和权限正确。
- [ ] `push_message_group`和`alarm_rank=1/2/3`通过System接口可查。
- [ ] Redis `sys_dict2:push_message_group`、`sys_dict2:alarm_rank`存在。
- [ ] 生产两个内部测试桩均为false。
- [ ] 第一次启动Push过滤开关为false。
- [ ] 企业微信启用时密钥为Base64解码32字节；未启用时不强制配置。

## 4. 启动

- [ ] 先Push后Alarm。
- [ ] 两个服务均以Java 8、`-Dfile.encoding=UTF-8`启动。
- [ ] 两个服务注册到Nacos。
- [ ] Alarm actualDataNodes刷新成功，无actual table错误。
- [ ] Push目录和主动配置冷启动恢复成功。
- [ ] `alarm_queue`、`push.alarm`和动态队列消费者正常。

## 5. 功能矩阵

- [ ] HTTP和真实`alarm_queue`均可创建报警。
- [ ] 历史报警可按时间和新ID查询。
- [ ] Alarm处理、工单、转派和stop闭环通过。
- [ ] 过滤关闭时旧主动推送行为不变。
- [ ] 类型10等级1允许配置可接收。
- [ ] 类型10等级2/3按各配置排除。
- [ ] 类型10无等级按类型正常推送。
- [ ] 类型25不做等级过滤。
- [ ] 站内`TENANT_ALL`不受主动配置等级过滤影响。
- [ ] 企业微信仅在启用配置时验证最终实收。

## 6. 数据和证据

- [ ] API、数据库、RabbitMQ、Redis、最终接收端证据一致。
- [ ] MQ重复消息没有形成未解释的重复报警或重复成功投递。
- [ ] stop重复消息由事件唯一键和状态机保护。
- [ ] 测试tenantId、设备和Redis登录上下文均已清理。
- [ ] cleanup SQL只清理本轮唯一前缀数据。

## 7. 快速止损

- [ ] 已演练`push.routing.dict-exclude-filter-enabled=false`。
- [ ] 已演练`push.open=false`。
- [ ] 已记录Push、Alarm旧JAR和启动命令。
- [ ] 已记录数据库前滚修复原则，不依赖破坏性逆向DDL。

## 8. 最终结论

只有以上适用项全部勾选且阻断项为0，才允许标记PASS。依赖不可用写BLOCKED；接口失败、数据不一致或最终通道未到达写FAIL，不得用“服务已启动”代替业务验收。
