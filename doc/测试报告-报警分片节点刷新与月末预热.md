# 测试报告-报警分片节点刷新与月末预热

## 基本信息

- 测试日期：2026-06-08
- 测试分支：`codex/alarm-sharding-rule-refresh-test`
- 合并目标：`main`
- 测试范围：报警分片 actualDataNodes 显式注册、DataSource 刷新、动态建表后主动刷新、月末/快速预热链路、真实接口 CRUD 回归。

## 自动化测试

执行目录：`D:\studyProject\hpis2.0\hpis`

执行命令：

```powershell
mvn -pl hpis-alarm -am "-Dtest=AlarmIdCodecTest,AlarmCidIndexServiceTest,AlarmTimeCapacityShardingApiTest,AlarmMonthlySliceTableManagerActualDataNodesTest,RefreshableAlarmShardingDataSourceTest,AlarmShardingRuleRefreshJobTest,AlarmShardingRuleRefreshServiceTest,AlarmShardingNextMonthPreCreateJobTest" -DfailIfNoTests=false "-Dfile.encoding=UTF-8" test
```

结果摘要：

- 结果：通过
- Maven 结果：`BUILD SUCCESS`
- 测试数：`33`
- Failures：`0`
- Errors：`0`
- Skipped：`0`
- 结束时间：`2026-06-08T21:11:55+08:00`

覆盖断言：

- `preCreateMonths` 默认值为 `0`。
- 当前月 actualDataNodes 注册 `00..255`。
- 下个月 actualDataNodes 默认注册 `00..09`，不注册 `10`。
- 历史月份只注册数据库实际存在的 `yyyyMM_nn` 物理表。
- 不再生成 legacy `alarm_0..4`、`alarm_handle_0..4`、`alarm_electrolytic_cell_0..4`。
- 动态建表后主动刷新、去抖、afterCommit、回滚不刷新、单飞锁、刷新失败保护均通过。
- 月末预热任务在月末创建下月 `00` 后调用 `refreshNow("month-end-precreate")`。

## 打包验证

执行目录：`D:\studyProject\hpis2.0\hpis`

执行命令：

```powershell
mvn -pl hpis-alarm -am -DskipTests "-Dfile.encoding=UTF-8" package
```

结果摘要：

- 结果：通过
- Maven 结果：`BUILD SUCCESS`
- 输出产物：`D:\studyProject\hpis2.0\hpis\hpis-alarm\target\hpis-alarm.jar`
- 结束时间：`2026-06-08T21:11:48+08:00`

## 服务启动与节点刷新

启动要求：

- 使用 Java 8：`C:\Program Files\Java\jdk1.8.0_321\bin\java.exe`
- JVM 参数：`-Dfile.encoding=UTF-8`
- 测试端口：`18806`
- Nacos、MySQL、Redis 保持运行。
- Rabbit listener auto-startup 关闭，避免测试期间消费 MQ。
- 快速预热配置通过 `SPRING_APPLICATION_JSON` 传入，避免 PowerShell 拆分 cron 空格。

快速预热配置：

```json
{
  "server": { "port": 18806 },
  "spring": {
    "rabbitmq": {
      "listener": {
        "simple": { "auto-startup": false },
        "direct": { "auto-startup": false }
      }
    }
  },
  "alarm": {
    "internal-test": {
      "remote-call-stub-enabled": true
    },
    "sharding": {
      "pre-create-months": 0,
      "include-legacy-tables": false,
      "actual-data-nodes": {
        "next-month-max-slice-no": 9
      },
      "rule-refresh": {
        "month-end-pre-create-enabled": true,
        "month-end-pre-create-check-last-day": false,
        "month-end-pre-create-cron": "0/30 * * * * ?",
        "month-end-pre-create-next-month-max-slice-no": 0
      }
    }
  }
}
```

验证结果：

- 服务启动成功：`Started HpisAlarmApplication in 21.103 seconds`。
- 三张逻辑表启动时均输出 `alarm sharding actualDataNodes refreshed`，`actualTableCount=281`。
- 快速预热任务输出：`alarm sharding rule refresh success, reason=month-end-precreate`。
- 快速预热任务输出：`alarm sharding next month pre-create finished, today=2026-06-08, maxSliceNo=0, created=false, refreshed=true`。
- 未出现 `Actual table ... is not in table config`。
- 未出现 `Table ..._01 doesn't exist`。

说明：

- `created=false` 是因为测试库中 `202607_00` 三张物理表在本次快速预热前已经存在；本轮验证覆盖了定时触发和显式刷新 DataSource 闭环。
- 曾尝试使用 `--spring.aop.auto=false` 绕过鉴权，但会导致 `startWebSocketConnection` Bean 类型异常，已确认该方式不适用于本项目。
- 曾出现一次 cron 参数被 PowerShell 拆分导致 `Cron expression must consist of 6 fields`，后续改用 `SPRING_APPLICATION_JSON` 传递。

## 数据库验证

执行 SQL：

```sql
show tables like 'alarm_202606_00';
show tables like 'alarm_handle_202606_00';
show tables like 'alarm_electrolytic_cell_202606_00';

show tables like 'alarm_202607_00';
show tables like 'alarm_handle_202607_00';
show tables like 'alarm_electrolytic_cell_202607_00';

select month_key, slice_no, table_suffix
from alarm_shard_slice
where month_key in ('202606', '202607')
order by month_key, slice_no;
```

结果摘要：

- `alarm_202606_00`：存在。
- `alarm_handle_202606_00`：存在。
- `alarm_electrolytic_cell_202606_00`：存在。
- `alarm_202607_00`：存在。
- `alarm_handle_202607_00`：存在。
- `alarm_electrolytic_cell_202607_00`：存在。
- `alarm_shard_slice` 存在 `202606_00` 和 `202607_00` 元数据。

## 真实接口 CRUD 回归

测试前置：

- 使用 Redis 临时写入 `login_tokens:codex-crud-token-20260608`，权限包含 `*:*:*`，用于通过 `query/edit/delete` 上的 `PreAuthorize`。
- 使用现有设备缓存：`device_sn2:HM-TD2068T-5/Q20250724AACHEA4925334`，其 `tenantId=123`，与列表查询硬编码 tenant 条件一致。
- 启动参数开启 `alarm.internal-test.remote-call-stub-enabled=true`，避免本地未启动 `hpis-device` 时新增接口卡在远程联动。
- 测试数据前缀：`codex-crud-20260608-*`。

执行接口：

```http
POST /alarm/alarmAdd
GET /alarm/list?pageNum=1&pageSize=5&startTime=2026-06-01%2000:00:00&endTime=2026-06-30%2023:59:59&deviceSn=HM-TD2068T-5/Q20250724AACHEA4925334
GET /alarm/query/{alarmId}
PUT /alarm
DELETE /alarm/{alarmId}
```

测试数据：

- `alarmCid`：`codex-crud-20260608-20260608215311`
- `alarmId`：`661466195270042600`
- `deviceSn`：`HM-TD2068T-5/Q20250724AACHEA4925334`
- `alarm_beginTime`：`2026-06-08 10:20:30`

结果摘要：

- 新增：HTTP `200`，数据库 `alarm_202606_00` 查到测试数据。
- 列表分页：HTTP `200`，响应包含测试 `alarmCid`，确认列表接口使用 `startTime/endTime` 可正常命中当前月分片。
- 单条查询：HTTP `200`，响应包含测试 `alarmCid`。
- 修改：HTTP `200`，`target_name` 更新为 `codex-crud-updated`。
- 删除：HTTP `200`，删除后 `alarm_202606_00` 中该 `alarmId` 数量为 `0`。
- 清理：已清理 `alarm_handle_202606_00`、`alarm_electrolytic_cell_202606_00`、`alarm_cid_index`、`alarm_cid_stale_index` 中本次测试数据，最终计数均为 `0`。

备注：

- `remarkData` 在本次 PUT 后未落到 `remark_data`，但 `targetName` 修改已生效；本轮验收目标是验证“修改接口可改目标字段之一”，已满足。
- `/alarm/alarmAdd` 会吞异常并返回 HTTP `200`，因此新增接口必须以数据库反查作为成功标准。

## 前端参数确认

- `/alarm/list` 时间范围按接口约定使用 `BaseEntity.startTime/endTime`。
- 已更新 `src/test/resources/http/alarm-time-capacity-sharding-api.http`，列表分页示例改为 `startTime/endTime`。
- 已更新 `doc/alarm-time-capacity-interface-test.md`，明确不要使用 `alarmBegintime/alarmEndtime` 作为列表分页查询入参。
- 使用 `alarmBegintime/alarmEndtime` 时，`AlarmServiceImpl.selectAlarmPage` 不会拼接时间条件，可能广播到当前月已注册但未物理创建的 slice。

## 环境配置核对

- 代码默认值符合设计：`preCreateMonths=0`，当前月注册 `00..255`，下个月注册 `00..09`。
- 当前 Nacos `hpis-alarm-dev.yml` 中仍存在旧配置：`preCreateMonths: 2`、`includeLegacyTables: true`。
- 本轮测试通过启动覆盖参数验证新设计；正式环境建议同步清理旧配置，避免运行行为和文档默认值不一致。

## 未覆盖项和风险

- 未覆盖真实“下个月 00 从无到有创建并返回 `created=true`”场景，因为测试库中下月三张表已存在。
- 未验证多实例并发建表互斥；当前服务按单实例设计，本轮不引入分布式锁。
- 真实 CRUD 使用本地 remote-call stub，验证的是 alarm 内部 DB/分片/接口闭环，不代表 hpis-device、WebSocket、推送等跨服务联动已完成联调。
- Nacos 旧配置需要后续运维同步，否则可能覆盖代码默认行为。

## 最终结论

- 自动化测试：通过。
- 打包验证：通过。
- 服务启动与节点刷新：通过。
- 数据库状态验证：通过。
- 真实接口 CRUD：通过。
- 测试数据清理：通过。
- 是否允许合并到 `main`：允许。

## 合并结果

- 功能分支提交：`2d08d76 feat: refresh alarm sharding data nodes`。
- 已执行非快进合并到 `main`：`git merge --no-ff codex/alarm-sharding-rule-refresh-test`。
- 合并后聚焦回归命令：

```powershell
mvn -pl hpis-alarm -am "-Dtest=AlarmIdCodecTest,AlarmCidIndexServiceTest,AlarmTimeCapacityShardingApiTest,AlarmMonthlySliceTableManagerActualDataNodesTest,RefreshableAlarmShardingDataSourceTest,AlarmShardingRuleRefreshJobTest,AlarmShardingRuleRefreshServiceTest,AlarmShardingNextMonthPreCreateJobTest" -DfailIfNoTests=false "-Dfile.encoding=UTF-8" test
```

- 合并后测试结果：`BUILD SUCCESS`，`Tests run: 33, Failures: 0, Errors: 0, Skipped: 0`。
- 合并后测试结束时间：`2026-06-08T22:14:44+08:00`。
