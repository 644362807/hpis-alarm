# 报警 2w MQ 每分钟 Phase B 验证报告

## 结论

Phase B 电解槽快照异步投影已经通过数据正确性回归，但严格性能门禁仍未通过，因此本轮不合并 `main`。

系统实际运行已经接近目标：持续输入期间 RabbitMQ `ready max=0`，stop event `PENDING max=249`，没有线性积压。未达标差距主要来自 route 可见性重试、verifier 终态完整 count 和环境 MyBatis DEBUG 日志 I/O。

下一阶段只执行一个主任务：**降低 route 可见性竞争与无效重试**。实施计划见 `doc/报警2wMQ每分钟PhaseB后续拆分计划-20260602.md`。

## 环境

- 分支：`test/alarm-ec-snapshot-outbox`
- RabbitMQ：`127.0.0.1:5672`，队列 `alarm_queue`
- MySQL：`127.0.0.1:3306/hpis_alarm`
- Java：JDK 8，`-Dfile.encoding=UTF-8`
- consumer batch：`batchSize=100`、`prefetch=100`、`concurrency=2-4`
- stop worker：`workerThreads=4`、`claimBatchSize=200`、`maxInFlightBatches=4`
- stop 初始可见延迟：`200ms`
- snapshot worker：`workerThreads=2`、`claimBatchSize=100`、`maxInFlightBatches=2`
- snapshot 初始可见延迟：`2000ms`

## Focused 测试

从父工程执行：

```powershell
mvn -pl hpis-alarm -am -DfailIfNoTests=false "-Dfile.encoding=UTF-8" test
mvn -pl hpis-alarm -am -DskipTests "-Dfile.encoding=UTF-8" package
```

结果：`82` 个测试执行，`0` 失败，`0` 错误，`2` 跳过；打包通过。

## 短时真实 MQ + MySQL 回归

每类场景均先执行 warm-up，再执行正式轮次。

| 场景 | Run ID | MQ 数量 | 核心闭环 | 关闭吞吐 | 结果 |
| --- | --- | ---: | ---: | ---: | --- |
| `GENERAL_ALTERNATE` | `P2W-GALT-FORMAL-20260602022905` | `20000` | `14828ms` | `674.40 alarm/s` | PASS |
| `ELECTROLYTIC_ALTERNATE` | `P2W-EALT-FORMAL-20260602022952` | `20000` | `27323ms` | `365.99 alarm/s` | PASS |
| `MIXED_ALTERNATE` | `P2W-MALT-FORMAL-20260602023051` | `20000` | `18887ms` | `529.46 alarm/s` | PASS |

`MIXED_ALTERNATE 20000 MQ` 满足核心闭环 `<= 120s`。

## 持续输入门禁

Run ID：`P2W-MALT-SUSTAINED-20260602023203`

| 指标 | 结果 | 门禁 | 判定 |
| --- | ---: | ---: | --- |
| 输入 MQ | `200000` | `20000 MQ/min x 10min` | PASS |
| 发送耗时 | `600176ms` | `<= 600000ms` | FAIL，边界差 `176ms` |
| 发送吞吐 | `333.24 MQ/s` | `>= 333.33 MQ/s` | FAIL，边界差 `0.09 MQ/s` |
| 核心闭环耗时 | `609475ms` | `<= 600000ms` | FAIL，边界差 `9475ms` |
| 核心关闭吞吐 | `164.08 alarm/s` | `>= 166.67 alarm/s` | FAIL，边界差 `2.59 alarm/s` |
| RabbitMQ 最大 ready | `0` | 不线性增长 | PASS |
| stop event 最大 PENDING | `249` | 不线性增长 | PASS |
| stop event 最大 PROCESSING | `136` | 可回落 | PASS |
| stop event 终态 | `APPLIED=100000` | 无积压、无失败 | PASS |
| 电解槽扩展表 | `45000` | ID 一致、非空 | PASS |
| route 终态 | `CLOSED=100000` | 全部关闭 | PASS |
| side effect 最终空闲后终态 | `DONE=55000` | 最终清空 | PASS |
| snapshot 命令空闲后终态 | `DONE/DELETED=48` | 无开放、无失败 | PASS |

发送结束后报告采样到 `DB closed elapsed after send=9299ms`。终态完整 count 会给 MySQL 增加额外负载，因此报告吞吐是带 verifier 负载的闭环吞吐。

## 异常与风险信号

以下统计覆盖本次服务启动后的短矩阵和持续门禁：

| 信号 | 次数 | 结论 |
| --- | ---: | --- |
| `Deadlock found` | `0` | PASS |
| snapshot claim deadlock retry | `0` | PASS |
| `PERSIST_LOCK_RETRY` | `0` | PASS |
| `PERSIST_FALLBACK` | `0` | PASS |
| 快照投影批次失败 | `0` | PASS |
| snapshot superseded | `0` | 本轮未触发，已有防护和单测 |
| `ROUTE_MISSING_RETRY` | `1093` | 仍需治理 |

应用日志约 `310.96MB`。命令行已经请求关闭 MyBatis DEBUG，但 Nacos 环境配置仍然覆盖为 DEBUG。性能环境和生产灰度必须关闭该日志级别。

## 最终一致性 SQL 结果

- MQ：`ready=0`、`unacked=0`
- stop event：`APPLIED=100000`
- side effect：`DONE=55000`
- snapshot command：`DONE/DELETED=48`
- snapshot command：`PENDING=0`、`PROCESSING=0`、`FAILED=0`
- 已 stop 且未被新报警替代的点位：无残留投影

## 未继续放大的测试

按计划，持续输入严格门禁未通过时不继续执行 `50000 / 100000 MQ` 突发恢复和独立 `750000 PENDING` 放大测试，避免重复放大已知问题。

## 判定

当前代码是可继续灰度验证的测试候选，不是可合并 `main` 的生产化候选。下一轮先治理 route 可见性竞争和 verifier 测量隔离，再重新执行同一门禁。
