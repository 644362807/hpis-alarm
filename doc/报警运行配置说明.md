# 报警运行配置说明

## 文档定位

本文说明 `hpis-alarm` 当前运行期 `alarm.*` 配置的真实绑定情况、默认值、是否必须配置、执行入口和上线风险。

结论先说清楚：

- `alarm.sharding.*` 是有效配置，绑定到 `AlarmShardProperties`，控制时间 + 容量分片、cid 热点路由和内部 alarm_id 编码。
- `alarm.stop-worker.*` 是有效配置，绑定到 `AlarmStopWorkerProperties`，控制可靠消警 worker、side effect worker 和 APPLIED 清理。
- `alarm.batch.*` 是有效配置，绑定到 `AlarmBatchProperties`，控制 insert/stop 内部批量 API、SQL IN 边界和批量失败兜底。
- `alarm.sql-log.*` 是有效配置，绑定到 `AlarmSqlLogProperties`，控制自定义 MyBatis SQL 日志拦截器是否打印 SQL。
- `alarm.profile.*` 当前代码没有任何读取入口，属于无效配置或历史压测残留；保留在 Nacos 不会生效。
- 本地 `bootstrap.yml` 只负责连接 Nacos。实际运行配置通常来自 Nacos 的 `hpis-alarm-${profile}.yml` 或共享 `application-${profile}.yml`。

## 推荐注释版配置

```yaml
alarm:
  sharding:
    # 是否启用 hpis-alarm Java API 时间 + 容量分片。
    # false 或缺失时，不创建新分片 DataSource，也不会启用 cid lifecycle job、stop worker。
    # 生产开启前必须确认旧 alarm_id % 5 inline 分片规则已关闭，避免两套分片规则同时生效。
    enabled: true

    # 单个月内单个子表最大预占行数。
    # 到达阈值后，当前月份会切到下一个 nn 子表，例如 alarm_202605_01。
    # 代码有边界校验：必须 > 0，且不能超过 8388608。
    maxRowsPerSlice: 5000000

    # 预创建未来月份数量。
    # 代码实际会创建当前月 + 未来 N 个月的 00 子表。
    preCreateMonths: 2

    # 本机每次从 alarm_shard_slice 预占的 rowNo 号段大小。
    # 值越大，数据库锁竞争越低，但服务异常退出时可能留下更多未使用空洞。
    # 配置小于 1 时，代码会用 Math.max(1, value) 兜底为 1。
    allocationSegmentSize: 1000

    # 迁移期是否把旧 alarm_0..4 / alarm_handle_0..4 / alarm_electrolytic_cell_0..4 纳入查询兜底。
    # 旧数据未迁移完成时保持 true；迁移完成并回填路由后建议改 false，减少查询扫描范围。
    includeLegacyTables: true

    id:
      # 内部 alarm_id 编码中的 workerId，占 8 bit，有效范围 0..255。
      # 单实例可用 0；多实例生产部署必须为每个实例配置不同值，否则极端情况下可能出现 ID 冲突。
      workerId: 0

    cidIndex:
      # 是否打印 cid 路由生命周期定时任务的普通完成日志。
      # 生产默认 false，只保留异常日志；排查 hot/stale 流转时可临时开启。
      logEnabled: false

      # ACTIVE hot 路由保留小时数。超过后，低流量任务转入 alarm_cid_stale_index。
      hotHours: 24

      # stale 路由保留天数。超过后，任务会先关闭业务报警，再关闭路由。
      staleExpireDays: 30

      # CLOSED hot/stale 路由清理批大小。
      cleanupBatchSize: 1000

      # hot 转 stale 每批处理数量。
      transferBatchSize: 1000

      # CLOSED 路由物理清理任务 cron。
      cleanupCron: "0 0 2 * * ?"

      # hot 转 stale 任务 cron。
      transferCron: "0 10 2 * * ?"

      # stale 超时关闭任务 cron。
      staleTimeoutCron: "0 20 2 * * ?"

  stop-worker:
    # 是否打印 stop worker 普通完成日志。
    # 生产默认 false，避免高频调度刷屏；排查处理节奏时可临时开启。
    logEnabled: false

    # PENDING stop event 数量 <= 该值时视为低流量，允许执行 APPLIED 清理。
    lowWatermark: 500

    # PENDING stop event 数量 >= 该值时进入高流量模式，核心消警 worker 会加大批量和循环次数。
    highWatermark: 5000

    # 正常流量每批处理 PENDING stop event 数量。
    normalBatchSize: 500

    # 高流量每批处理 PENDING stop event 数量。
    highBatchSize: 2000

    # 核心 stop worker 和 side effect worker 调度间隔，单位毫秒。
    normalIntervalMs: 1000

    # 当前代码没有直接作为 @Scheduled 间隔使用。
    # 高流量实际通过 highBatchSize + maxParallelism 提升单轮处理能力。
    highIntervalMs: 100

    # 高流量模式下单轮最多连续处理批次数。
    maxParallelism: 4

    # 是否生成并执行消警后的副作用事件，例如设备在线状态恢复、电解槽扩展清理。
    # 关闭后只保证核心 alarm_endTime 和 cid route 关闭。
    sideEffectEnabled: true

    # APPLIED stop event 保留分钟数，到期后在低流量窗口清理。
    appliedRetentionMinutes: 30

    # APPLIED stop event 清理批大小。
    cleanupBatchSize: 1000

    # APPLIED stop event 清理扫描间隔，单位毫秒。
    cleanupIntervalMs: 60000

    # FAILED 事件保留天数。当前代码只记录该配置，不会自动删除 FAILED。
    failedRetentionDays: 7

    # true 表示只在低流量模式清理 APPLIED stop event。
    cleanupOnlyLowTraffic: true

    # 单条 stop event 或 side effect event 最大重试次数，达到后转 FAILED。
    maxRetry: 5

    # 是否启用 stop worker 空闲暂停。
    # true 时，连续空轮次后 @Scheduled 仍触发，但只做内存判断，不再每秒查 stop event 表。
    idlePauseEnabled: true

    # 连续多少轮核心消警 + 副作用都无处理量后进入 idle pause。
    idleConfirmCount: 3

    # idle pause 后低频兜底查库间隔，单位毫秒。
    # 用于防止重启、漏唤醒、多实例场景下 PENDING 长期卡住。
    idleProbeIntervalMs: 60000

  batch:
    # 是否启用 stop worker 的批量 route 查询和按 suffix 分组消警。
    # 关闭后回到旧的单条 route 查询链路；旧链路不会把 route 缺失直接转 FAILED。
    stopEnabled: true

    # 是否启用内部 insertAlarms(List<JSONObject>) 批量持久化 API。
    # 现有 MQ/Controller 单条 insertAlarm(JSONObject) 入口不受影响。
    insertEnabled: true

    # 单次 SQL IN 查询或批量 upsert 的分片上限。
    # 当前默认 500；生产可根据数据库参数上限和压测结果调整。
    inLimit: 500

    # 批量 route 查询或批量 insert 失败时是否降级单条处理。
    # insert 批量降级会先回滚批量事务，再逐条开启独立事务重放，避免半批成功后重复写。
    fallbackSingleOnBatchError: true

    # 是否启用 RabbitMQ consumer batch listener。默认 false，保持旧 RabbitMQAlarmListener。
    # 开启后旧 listener 会被条件关闭，RabbitMQAlarmBatchListener 接管 alarm_queue。
    insertConsumerBatchEnabled: false

    # RabbitMQ 容器单次最多收集多少条消息后调用 batch listener。
    # 注意这是容器真实批量，不是内存 submitAndWait 聚合；建议第一阶段 100 起步。
    insertConsumerBatchSize: 100

    # 容器等待凑批的最长时间，单位毫秒。低流量下该值直接影响 start ack 延迟。
    insertConsumerBatchReceiveTimeoutMs: 50

    # batch listener 并发，格式为 "min-max" 或单个数字。
    # 第一阶段建议 2-4，压测后再放大，避免 DB 连接池和 Redis 被瞬时打满。
    insertConsumerBatchConcurrency: "2-4"

    # 每个 batch consumer 的 Rabbit prefetch。必须不小于 batchSize，否则实际批次无法打满。
    # 配置小于 batchSize 时，代码会按 batchSize 托底。
    insertConsumerBatchPrefetch: 100

    # 普通 batch listener 成功日志开关。false 时失败、nack、坏消息 reject 仍会 warn/error。
    insertConsumerBatchLogEnabled: false

  sql-log:
    # 自定义 MyBatis SQL info 日志总开关。
    # false 时不打印任何 SQL info；warn/error 异常日志仍保留。
    enabled: false

    # SQL 打印模式：
    # all：打印所有 SQL，等同旧行为，生产不建议长期开启。
    # alarm-write：只打印报警主链路写 SQL，并额外打印慢 SQL；stop-worker SQL 默认排除。
    # slow：只打印慢 SQL，stop-worker SQL 默认排除。
    mode: alarm-write

    # 是否打印参数和 Full SQL。
    # 生产涉及敏感字段时建议 false。
    printParam: false

    # enabled=true 时，是否允许慢 SQL 日志。
    slowEnabled: true

    # 慢 SQL 阈值，单位毫秒。
    slowMs: 200

  profile:
    # 当前代码没有读取 alarm.profile.* 的入口。
    # 这组配置不会影响运行行为，建议从生产 Nacos 删除，或后续恢复 profiling 组件后再启用。
    enabled: false
    methodEnabled: false
    runId: batch-electro-20k-20260514-001
    logIntervalMs: 5000
    slowSqlMs: 50
    summaryTopN: 30
    methodSlowMs: 100
    methodSlowLogEnabled: true
```

## 配置绑定和默认值

| 配置 | 是否有效 | 是否必须 | 默认值/兜底 | 绑定位置 |
| --- | --- | --- | --- | --- |
| `alarm.sharding.enabled` | 有效 | 切新分片时必须显式 true | 默认 `false` | `AlarmShardProperties.enabled` |
| `alarm.sharding.maxRowsPerSlice` | 有效 | 启用分片后建议显式配置 | 默认 `5000000`；必须 `1..8388608` | `AlarmShardProperties.maxRowsPerSlice`、`AlarmIdCodec.validateConfiguredBounds` |
| `alarm.sharding.preCreateMonths` | 有效 | 非必须 | 默认 `1`；代码实际预建当前月到未来 N 月 | `AlarmMonthlySliceTableManager.preCreateMonthSlices` |
| `alarm.sharding.allocationSegmentSize` | 有效 | 非必须 | 默认 `1000`；小于 1 时按 1 使用 | `AlarmMonthlySliceTableManager.getAllocationSegmentSize` |
| `alarm.sharding.includeLegacyTables` | 有效 | 迁移期建议显式配置 | 默认 `true` | `AlarmMonthlySliceTableManager.listTablesByTimeRange` |
| `alarm.sharding.id.workerId` | 有效 | 多实例必须显式配置 | 默认 `0`；必须 `0..255` | `AlarmIdCodec.nextId` |
| `alarm.sharding.cidIndex.logEnabled` | 有效 | 非必须 | 默认 `false` | `AlarmCidIndexLifecycleJob` |
| `alarm.sharding.cidIndex.hotHours` | 有效 | 非必须 | 默认 `24` | `AlarmCidIndexService.transferHotToStale` |
| `alarm.sharding.cidIndex.staleExpireDays` | 有效 | 非必须 | 默认 `30` | `AlarmCidIndexService.transferHotToStale` |
| `alarm.sharding.cidIndex.cleanupBatchSize` | 有效 | 非必须 | 默认 `1000` | `AlarmCidIndexService.cleanupClosedRoutes`、`findExpiredActiveStaleRoutes` |
| `alarm.sharding.cidIndex.transferBatchSize` | 有效 | 非必须 | 默认 `1000` | `AlarmCidIndexService.transferHotToStale` |
| `alarm.sharding.cidIndex.cleanupCron` | 有效 | 非必须 | 默认 `0 0 2 * * ?` | `AlarmCidIndexLifecycleJob.cleanupClosedRoutes` |
| `alarm.sharding.cidIndex.transferCron` | 有效 | 非必须 | 默认 `0 10 2 * * ?` | `AlarmCidIndexLifecycleJob.transferHotToStale` |
| `alarm.sharding.cidIndex.staleTimeoutCron` | 有效 | 非必须 | 默认 `0 20 2 * * ?` | `AlarmCidIndexLifecycleJob.closeExpiredStaleRoutes` |
| `alarm.stop-worker.logEnabled` | 有效 | 非必须 | 默认 `false` | `AlarmStopWorkerProperties.logEnabled` |
| `alarm.stop-worker.lowWatermark` | 有效 | 非必须 | 默认 `500` | `AlarmStopEventService.isLowTraffic` |
| `alarm.stop-worker.highWatermark` | 有效 | 非必须 | 默认 `5000` | `AlarmStopEventService.isHighTraffic` |
| `alarm.stop-worker.normalBatchSize` | 有效 | 非必须 | 默认 `500` | `AlarmStopEventService.processPendingBatch` |
| `alarm.stop-worker.highBatchSize` | 有效 | 非必须 | 默认 `2000` | `AlarmStopEventService.processPendingBatch` |
| `alarm.stop-worker.normalIntervalMs` | 有效 | 非必须 | 默认 `1000` | `AlarmStopEventWorker.processStopEvents/processSideEffects` |
| `alarm.stop-worker.highIntervalMs` | 部分有效 | 非必须 | 默认 `100`；当前只保存在配置对象中，未直接参与调度 | `AlarmStopWorkerProperties.highIntervalMs` |
| `alarm.stop-worker.maxParallelism` | 有效 | 非必须 | 默认 `4` | `AlarmStopEventService.currentBatchLoops` |
| `alarm.stop-worker.sideEffectEnabled` | 有效 | 非必须 | 默认 `true` | `AlarmStopSideEffectService`、`AlarmStopEventWorker.processSideEffects` |
| `alarm.stop-worker.appliedRetentionMinutes` | 有效 | 非必须 | 默认 `30` | `AlarmStopEventService.buildDeleteAfter` |
| `alarm.stop-worker.cleanupBatchSize` | 有效 | 非必须 | 默认 `1000` | `AlarmStopEventService.cleanupAppliedIfAllowed` |
| `alarm.stop-worker.cleanupIntervalMs` | 有效 | 非必须 | 默认 `60000` | `AlarmStopEventWorker.cleanupAppliedEvents` |
| `alarm.stop-worker.failedRetentionDays` | 部分有效 | 非必须 | 默认 `7`；当前未执行 FAILED 自动清理 | `AlarmStopWorkerProperties.failedRetentionDays` |
| `alarm.stop-worker.cleanupOnlyLowTraffic` | 有效 | 非必须 | 默认 `true` | `AlarmStopEventService.cleanupAppliedIfAllowed` |
| `alarm.stop-worker.maxRetry` | 有效 | 非必须 | 默认 `5` | `AlarmStopEventService.markRetryOrFailed`、`AlarmStopSideEffectService.markRetryOrFailed` |
| `alarm.stop-worker.idlePauseEnabled` | 有效 | 非必须 | 默认 `true` | `AlarmStopWorkerSignal.shouldRunCycle` |
| `alarm.stop-worker.idleConfirmCount` | 有效 | 非必须 | 默认 `3`；小于 1 时按 1 使用 | `AlarmStopWorkerSignal.afterCycle` |
| `alarm.stop-worker.idleProbeIntervalMs` | 有效 | 非必须 | 默认 `60000`；小于 1 时按 1 使用 | `AlarmStopWorkerSignal.shouldRunCycle` |
| `alarm.batch.stopEnabled` | 有效 | 非必须 | 默认 `true`；false 时 stop worker 回到旧单条链路 | `AlarmStopEventService.processPendingBatch` |
| `alarm.batch.insertEnabled` | 有效 | 非必须 | 默认 `true`；false 时 `insertAlarms` 逐条走旧链路 | `AlarmServiceImpl.insertAlarms` |
| `alarm.batch.inLimit` | 有效 | 非必须 | 默认 `500`；小于等于 0 时按 500 使用 | `AlarmBatchProperties.safeInLimit`、`AlarmCidIndexService.find*ByCids` |
| `alarm.batch.fallbackSingleOnBatchError` | 有效 | 非必须 | 默认 `true`；批量失败时走单条兜底 | `AlarmStopEventService`、`AlarmServiceImpl.insertAlarms` |
| `alarm.batch.insertConsumerBatchEnabled` | 有效 | 非必须 | 默认 `false`；true 时旧 `RabbitMQAlarmListener` 关闭，batch listener 接管 `alarm_queue` | `RabbitMQAlarmBatchListener` |
| `alarm.batch.insertConsumerBatchSize` | 有效 | 非必须 | 默认 `100`；小于等于 0 时按 100 使用 | `RabbitMQAlarmBatchListener.alarmBatchRabbitListenerContainerFactory` |
| `alarm.batch.insertConsumerBatchReceiveTimeoutMs` | 有效 | 非必须 | 默认 `50`；小于等于 0 时按 50 使用 | `RabbitMQAlarmBatchListener.alarmBatchRabbitListenerContainerFactory` |
| `alarm.batch.insertConsumerBatchConcurrency` | 有效 | 非必须 | 默认 `2-4`；格式错误时按 `2-4` 托底 | `AlarmBatchProperties.safeInsertConsumerBatchConcurrencyRange` |
| `alarm.batch.insertConsumerBatchPrefetch` | 有效 | 非必须 | 默认 `100`；小于 batchSize 时按 batchSize 托底 | `AlarmBatchProperties.safeInsertConsumerBatchPrefetch` |
| `alarm.batch.insertConsumerBatchLogEnabled` | 有效 | 非必须 | 默认 `false`；失败、nack、reject 不受该开关影响 | `RabbitMQAlarmBatchListener`、`AlarmInsertBatchConsumeService` |
| `alarm.sql-log.enabled` | 有效 | 非必须 | 默认 `false` | `CustomSqlLogInterceptor.shouldLog` |
| `alarm.sql-log.mode` | 有效 | 非必须 | 默认 `alarm-write`；支持 `all`、`alarm-write`、`slow` | `CustomSqlLogInterceptor.shouldLog` |
| `alarm.sql-log.printParam` | 有效 | 非必须 | 默认 `false` | `CustomSqlLogInterceptor.buildSqlLog` |
| `alarm.sql-log.slowEnabled` | 有效 | 非必须 | 默认 `true` | `CustomSqlLogInterceptor.shouldLog` |
| `alarm.sql-log.slowMs` | 有效 | 非必须 | 默认 `200` | `CustomSqlLogInterceptor.shouldLog` |
| `alarm.profile.*` | 当前无效 | 不需要 | 不生效 | 当前代码未读取 |

## 执行入口

### 配置加载入口

- `bootstrap.yml`：只配置服务名、profile、Nacos 地址和共享配置。
- Nacos：实际放置 `alarm.*` 的地方，通常是 `hpis-alarm-${profile}.yml` 或共享的 `application-${profile}.yml`。
- `HpisAlarmApplication`：通过 `@EnableScheduling` 开启定时任务。

### 分片入口

- `AlarmDynamicShardingConfig`
  - 条件：`alarm.sharding.enabled=true`
  - 作用：创建 Java API ShardingSphere 数据源、`AlarmMonthlySliceTableManager`、`AlarmCidIndexService`、`AlarmIdCodec`、`AlarmTimeCapacityShardingAlgorithm`。

- `AlarmServiceImpl.insertAlarm`
  - 新增报警入口。
  - 调用 `prepareAlarmShard` 分配 `tableSuffix`，通过 `AlarmShardContext` 固定本次 `alarm/alarm_handle/alarm_electrolytic_cell` 三张表落到同一分片。
  - 写入主表后调用 `AlarmCidIndexService.saveActiveHotRoute` 保存 hot cid 路由。

- `AlarmServiceImpl.insertAlarms`
  - 内部批量新增报警 API，外部 MQ/Controller 单条入口不变。
  - 受 `alarm.batch.insertEnabled` 控制；按分片 suffix 分组后批量写主表、处理表、扩展表和 hot cid route。
  - 批量异常时受 `alarm.batch.fallbackSingleOnBatchError` 控制，先回滚批量事务，再逐条单独事务兜底；兜底复用已准备好的 `AlarmInsertContext`，不会重新执行断线 Redis 去重和组装副作用。
- `RabbitMQAlarmBatchListener`
  - MQ start consumer batch 入口，仅在 `alarm.batch.insertConsumerBatchEnabled=true` 时启用；旧 `RabbitMQAlarmListener` 会被条件关闭，两个入口不会同时消费 `alarm_queue`。
  - Spring AMQP 容器按 `insertConsumerBatchSize/ReceiveTimeoutMs/Concurrency/Prefetch` 收集一批 `Message` 后调用 listener。
  - 同一批消息先处理 start，进入 `AlarmInsertBatchConsumeService` 批量持久化；start ack/nack 完成后，再按原相对顺序处理 stop/status，降低 route 缺失误判。
  - 每条消息按自己的结果 `basicAck/basicNack/basicReject`，不使用 `multiple=true`，避免部分成功/失败时误 ack。

- `AlarmTimeCapacityShardingAlgorithm`
  - 查询/更新路由入口。
  - 支持按 `AlarmShardContext`、内部 `alarm_id`、外部 `alarm_cid`、时间范围、`device_sn`、`irms_sn` 路由。

### cid 生命周期入口

- `AlarmCidIndexLifecycleJob.cleanupClosedRoutes`
  - cron：`alarm.sharding.cid-index.cleanup-cron`
  - 清理 CLOSED hot/stale 路由。

- `AlarmCidIndexLifecycleJob.transferHotToStale`
  - cron：`alarm.sharding.cid-index.transfer-cron`
  - 将长期未关闭的 hot ACTIVE 路由转入 stale 表。

- `AlarmCidIndexLifecycleJob.closeExpiredStaleRoutes`
  - cron：`alarm.sharding.cid-index.stale-timeout-cron`
  - 对超过 stale 保留期的报警先写业务结束状态，再关闭路由。

### 消警可靠化入口

- `RabbitMQAlarmListener.listenMessage`
  - stop MQ 入口。
  - `alarm.sharding.enabled=true` 且 `AlarmStopEventService` 存在时，stop 消息先写 `alarm_stop_event`，成功后 MQ ack。

- `AlarmStopEventWorker.processStopEvents`
  - fixed delay：`alarm.stop-worker.normalIntervalMs`
  - 批量处理 PENDING stop event，按 `alarm.batch.inLimit` 分片查询 hot/stale route，写业务 `alarm_endTime`，关闭 cid route，标记 event APPLIED。
  - `idlePauseEnabled=true` 时，连续空轮次后进入 idle pause；空闲期间只做内存判断，低频按 `idleProbeIntervalMs` 兜底查库。
  - `alarm.batch.stopEnabled=false` 可回滚到旧单条 route 查询链路。

- `AlarmStopEventWorker.processSideEffects`
  - fixed delay：`alarm.stop-worker.normalIntervalMs`
  - `idlePauseEnabled=false` 时保持旧行为，独立定时执行设备恢复、电解槽扩展清理等非核心副作用事件。
  - `idlePauseEnabled=true` 时由 `processStopEvents` 同一轮顺带处理副作用，避免第二个高频查库入口。

- `AlarmStopEventWorker.cleanupAppliedEvents`
  - fixed delay：`alarm.stop-worker.cleanupIntervalMs`
  - 清理 APPLIED stop event。

- `CustomSqlLogInterceptor`
  - 拦截 MyBatis `query/update/batch`。
  - `alarm.sql-log.enabled=false` 时不打印 SQL info。
  - `mode=alarm-write` 时只打印 `AlarmMapper.insertAlarm/insertAlarmBatch/updateAlarm/alarmStop*` 等报警主链路写 SQL；`AlarmStopEventMapper`、`AlarmStopSideEffectMapper`、`AlarmMapper.batchStopByAlarmIds` 等 stop-worker SQL 不打印。

## 必须满足的上线前提

1. `alarm.sharding.enabled=true` 前必须执行 `src/main/resources/sql/alarm-time-capacity-sharding.sql`。
2. 必须确认旧 ShardingSphere inline 规则已经关闭，否则会出现两套分片数据源同时生效。
3. 多实例部署必须配置不同的 `alarm.sharding.id.workerId`。
4. 迁移期旧数据未处理完时保持 `alarm.sharding.includeLegacyTables=true`。
5. `alarm_cid_index.uk_alarm_cid` 默认要求 `alarm_cid` 全局唯一；如果现场存在同 cid 多设备复用，必须按 SQL 注释改成 `(alarm_cid, device_sn)` 维度并同步代码评估。
6. `alarm.profile.*` 不要作为生产开关依赖；它当前没有执行入口。

## 有误配置的表现

- `alarm.sharding.enabled` 缺失或 false：新分片、cid lifecycle job、stop worker 都不会启用。
- `alarm.sharding.maxRowsPerSlice <= 0`：应用启动创建 `AlarmIdCodec` 时失败。
- `alarm.sharding.maxRowsPerSlice > 8388608`：应用启动创建 `AlarmIdCodec` 时失败。
- `alarm.sharding.id.workerId` 不在 `0..255`：应用启动或生成 ID 时失败。
- cron 写错：对应 `@Scheduled` 任务无法按预期启动。
- `alarm.stop-worker.highIntervalMs` 配置错误：当前不会直接影响调度，因为代码未使用它作为 `@Scheduled` 间隔。
- `alarm.stop-worker.idleProbeIntervalMs` 配置过大：漏唤醒或多实例场景下 PENDING stop event 的兜底恢复会变慢。
- `alarm.batch.inLimit` 配置过大：可能触发数据库 SQL 长度、参数数量或执行计划抖动；建议先保持 500，再按压测调整。
- `alarm.batch.stopEnabled=false`：可以回滚批量 route 查询，但 route 真缺失转 FAILED 的新语义也会被关闭，旧链路会继续保留 PENDING 等 start 补偿。
- `alarm.batch.insertEnabled=false`：内部批量新增 API 会退回逐条插入；现有单条 `insertAlarm` 本来就不受批量开关影响。
- `alarm.batch.insertAggregate*`：自研聚合链路已删除，旧 Nacos 残留配置不会再被代码读取；请从配置中心清理，避免误以为还有第三条 MQ start 链路。
- `alarm.batch.insertConsumerBatchEnabled=true`：MQ start 改走 consumer batch listener；必须确保旧 listener 未同时启动，并通过 RabbitMQ management 确认消费者数符合 `insertConsumerBatchConcurrency`。
- `alarm.batch.insertConsumerBatchPrefetch < insertConsumerBatchSize`：代码会按 batchSize 托底，否则 Rabbit 容器很难收满配置批量。
- `alarm.batch.insertConsumerBatchReceiveTimeoutMs` 配置过大：低流量下 ack 延迟会上升；配置过小：实际 batchSize 可能偏小，批量收益下降。
- `alarm.batch.insertConsumerBatchConcurrency` 配置过大：会放大 DB 连接池、Redis 和设备缓存访问压力；建议从 `2-4` 起步压测。
- `alarm.batch.insertConsumerBatchLogEnabled=false`：普通成功日志关闭，压测验证真实 `actualBatchSize` 时建议临时打开。
- `alarm.sql-log.enabled=false`：所有 SQL info 都不打印，排查 SQL 时需要临时改 true。
- `alarm.sql-log.mode=all`：恢复旧的全量 SQL 打印，高流量生产环境会明显放大日志 IO。
- `alarm.profile.*` 写错：当前无影响，因为没有代码读取。
